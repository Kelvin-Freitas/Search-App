<h2>Procurar usuário:</h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/findUser" method="POST">
            <div class="form-group">
                <input type="text" class="form-control" id="user" name="user" placeholder="Digite aqui o nome do usuário que procura" required>
            </div>
            <div class="d-flex justify-content-center mt-2">
                <button class="btn btn-success" type="submit">Procurar</button>
            </div>
        </form>
    </div>
</div>
<div class="row row-cols-1 row-cols-md-3 mt-4">
    {{#each users}}
        <div class="col mb-4">
            <div class="card">
                <img src="" class="card-img-top">
                <div class="card-body">
                    <h5 class="card-title">Usuário: {{name}}</h5>
                    <p class="card-text">Email: {{email}}</p>
                    <div class="d-flex justify-content-center">
                        <form action="/admin/deleteUser" method="POST">
                            <input type="hidden" id="email" name="email" value="{{email}}">
                            <input type="hidden" id="name" name="name" value="{{name}}">
                            <button type="submit" class="btn btn-danger mr-3">Banir conta</button>
                        </form>
                        <form action="/user/perfil" method="POST">
                            <input type="hidden" id="email" name="email" value="{{email}}">
                            <button type="submit" class="btn btn-primary mr-3">Perfil</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    {{else}}
    <div class="col mb-4">
        <div class="card">
            <div class="card-body">
                Desculpe, mas não encontramos nenhum usuário com esse nome!
            </div>
        </div>
    </div>
    {{/each}}
</div>