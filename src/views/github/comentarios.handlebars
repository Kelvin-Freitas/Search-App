<h2>Comentarios</h2>
<div class="card">
  <div class="card-body">
    <form method="POST" action="/github/store">
        <div class="form-group">
            <input type="hidden" id="tipo" name="tipo" value="{{tipo}}">
            <input type="hidden" id="id" name="id" value="{{id}}">
            <label for="textarea">Escreva aqui o seu comentário:</label>
            <textarea class="form-control" id="textarea" name="comment" rows="3"></textarea>
            <div class="d-flex justify-content-center mt-2">
                <button class="btn btn-success" type="submit">Enviar</button>
            </div>
        </div>
    </form>
  </div>
</div>
{{#if userAdm}}
    {{#each commentsList}}
        <div class="card">
            <div class="card-body">
                <blockquote class="blockquote mb-0">
                <p>{{comment}}</p>
                <footer class="blockquote-footer">{{user_id.name}}</footer>
                <form action="/admin/deletePost" method="POST">
                    <input type="hidden" id="id" name="id" value="{{_id}}">
                    <div class="float-right">
                        <button type="submit" class="btn btn-danger">Deletar</button>
                    </div><br>
                </form>
                </blockquote>
            </div>
        </div>
    {{else}}
        <div class="card">
            <div class="card-body">
                Que pena! Ainda não há comentários para exibir. Caso queira, seja o primeiro! 😆
            </div>
        </div>
    {{/each}}
{{else}}
    {{#each commentsList}}
        <div class="card">
            <div class="card-body">
                <blockquote class="blockquote mb-0">
                <p>{{comment}}</p>
                <footer class="blockquote-footer">{{user_id.name}}</footer>
                </blockquote>
                
            </div>
        </div>
    {{else}}
        <div class="card">
            <div class="card-body">
                Que pena! Ainda não há comentários para exibir. Caso queira, seja o primeiro! 😆
            </div>
        </div>
    {{/each}}
{{/if}}